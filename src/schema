-- Create a new table called 'xek_user' in schema 'xek'
-- Drop the table if it already exists
IF OBJECT_ID('xek.xek_user', 'U') IS NOT NULL
DROP TABLE xek.xek_user
GO
-- Create the table in the specified schema
CREATE TABLE xek.xek_user
(
  xek_userId INT NOT NULL PRIMARY KEY,
  -- primary key column
  Column1 [NVARCHAR](50) NOT NULL,
  Column2 [NVARCHAR](50) NOT NULL
  -- specify more columns here
);
GO